{
  "name": "adobe-crosshost-plugin-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Starter multi-host para Adobe Premiere Pro UXP e After Effects CEP/ExtendScript.",
  "scripts": {
    "clean": "node scripts/build.mjs --clean-only",
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm run validate && npm test && npm run skills:validate",
    "skills:sync": "node scripts/sync-agent-skills.mjs",
    "skills:validate": "node scripts/validate-agent-skills.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
