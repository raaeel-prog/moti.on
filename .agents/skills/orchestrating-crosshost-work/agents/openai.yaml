interface:
  display_name: "CrossHost Orchestrator"
  short_description: "Route work through the right design, host, and quality skills"
  brand_color: "#5B8CFF"
  default_prompt: "Use $orchestrating-crosshost-work for this task and follow its verification gates."
policy:
  allow_implicit_invocation: true
