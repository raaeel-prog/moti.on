{
  "name": "@motion/keyframe-core",
  "version": "0.1.0",
  "private": true,
  "description": "Snapshots canonicos, validacao fail-closed, comparacao tolerante, timebases e planos puros de restore de keyframes.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test \"tests/*.test.mjs\"",
    "test:coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-lines=80 --test-coverage-functions=80 --test-coverage-branches=80 \"tests/*.test.mjs\""
  },
  "license": "MIT"
}
